[user]
	email = mail@duncano.de
	name = Duncaen

[alias]
	l = log --pretty=oneline -n 20 --graph --abbrev-commit
	tags = tag -l
	branches = branch -a
	remotes = remote -v
	reb = rebase

[color]
	ui = auto

[push]
	default = simple

[core]
	pager = less -r
