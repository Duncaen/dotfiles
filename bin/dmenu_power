#!/bin/sh

: ${DMENU:=dmenu}
ret=$(echo "Suspend\nLock\nHibernate\nReboot\nShutdown" | ${DMENU} -p "Power:" -i | tr '[A-Z]' '[a-z]')
case "$ret" in
  suspend)   sudo zzz    ;;
  lock)      slock       ;;
  hibernate) sudo ZZZ    ;;
  reboot)    sudo reboot ;;
  shutdown)  sudo halt   ;;
esac
