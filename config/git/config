[user]
	email = mail@duncano.de
	name = Duncan Overbruck

[alias]
	l = log --pretty='format:%h %s%d' -n 20 --graph
	tags = tag -l
	branches = branch -a
	remotes = remote -v
	s = status -sb
	ss = s -uno

[color]
	ui = false
	diff = auto
